const darkTheme = {
    BACKGROUND: "#565661",       // Primary background
    SURFACE: "#44444d",          // Elevated surface background
    TEXT_PRIMARY: "#FAFAFA",      // Main text color on dark backgrounds
    TEXT_SECONDARY: "#ffffff87",    // Dimmed text for less emphasis
    TEXT_INVERT: "#1d1e1c",      // Text color when used on lighter elements
    BORDER_LIGHT: "#9e9eae",      // Lighter border for contrast
    BORDER_DARK: "#3a3a68",       // Darker border for depth
    ACCENT: "#292956",           // Accent color
    ERROR: "#D32F2F",            // Error state
    DISABLED: "#a0a0a56d"          // Disabled elements
};

const lightTheme = {
    BACKGROUND: "#F5F5F7",       // Primary background
    SURFACE: "#dedede",          // Elevated surface background
    TEXT_PRIMARY: "#1C1C1E",      // Main text color on light backgrounds
    TEXT_SECONDARY: "#1c1c1e75",    // Dimmed text for less emphasis
    TEXT_INVERT: "#FAFAFA",       // Text color when used on dark elements
    BORDER_LIGHT: "#b6b6be",      // Lighter border for subtle contrast
    BORDER_DARK: "#A0A0A5",       // Darker border for structure
    ACCENT: "#292956",           // Accent color
    ERROR: "#D32F2F",            // Error state
    DISABLED: "#C0C0C5"          // Disabled elements
};

export const colors = {
    darkTheme,
    lightTheme
}